#include "monty.h"

/**
 * monty_stack - sets the format of the data to a stack(LIFO)
 * @stack: Pointer tot he stack
 * @line_number: line number being executed
 */
void monty_stack(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
	que.f = 0;
}
